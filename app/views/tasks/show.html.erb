<div class="content">
	<h1 class="mt-5">
		<%= @task.title %>
	</h1>
	<p>
		<%= @task.body %>
	</p>
	<div class="my-3 p-3 bg-white rounded shadow-sm">
		<%= render @task.comments %>
	</div>
	<h3>Добавить комментарий</h3>
	<%= render 'comments/new' %>
</div>
